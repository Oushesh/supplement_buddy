## Entities in SeaORM Rust
    SeaORM --> Checks migration rust files 
    and translated that DSL File (Table::create())
    Raw SQL files
    --> Entities created

## SQL managed_migrations: which table were already run.
   Hidden file which tracks the migration status of seaOrm
   Only changes are applied. 

##  Database Structure Exists
    If database structure already exists then,
    just apply the Entites.

## Seeding some data:
    seed some data to test how it works.

    
    
    
    